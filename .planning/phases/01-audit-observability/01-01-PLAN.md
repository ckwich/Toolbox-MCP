---
phase: 01-audit-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - toolbox/models.py
  - toolbox/registry.py
  - toolbox/service.py
  - tests/test_service.py
autonomous: true
requirements:
  - OBS-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "Lifecycle operations persist audit events with timestamp, operation, outcome, and namespace where applicable"
    - "Runtime failures such as timeout or transport failure produce auditable structured events"
    - "Audit history survives service recreation and recent unregistration events from the same workspace state"
  artifacts:
    - path: "toolbox/models.py"
      provides: "Audit event model and typed query/result payloads"
      contains: "Audit"
    - path: "toolbox/registry.py"
      provides: "Persistent audit storage support"
      contains: "audit"
    - path: "toolbox/service.py"
      provides: "Lifecycle event emission"
      contains: "audit"
  key_links:
    - from: "toolbox/service.py"
      to: "toolbox/registry.py"
      via: "persisted audit writes"
      pattern: "audit"
---

<objective>
Create the audit event model and persistence path, then emit structured audit events from the current lifecycle and runtime-failure flows.

Purpose: The rest of the roadmap needs trustworthy observability before adding more behavior.
Output: Typed audit models, persistent storage support, lifecycle emission hooks, and regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-observability/01-CONTEXT.md
@spec.med
@toolbox/models.py
@toolbox/registry.py
@toolbox/service.py
@tests/test_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define audit models and persistent storage primitives</name>
  <files>toolbox/models.py, toolbox/registry.py</files>
  <read_first>toolbox/models.py, toolbox/registry.py, spec.med</read_first>
  <action>Add typed audit models aligned with the existing `ErrorInfo` and lifecycle vocabulary. Extend the JSON-backed persistence layer with bounded audit storage primitives that support append and filtered reads without introducing a second storage system unless that is clearly simpler and remains workspace-local. Keep the event shape compact: timestamp, operation, outcome, namespace, scope when relevant, current registration presence, and structured error metadata/details when present.</action>
  <verify>python -m pytest -q tests/test_service.py -k "audit or register or activate or refresh or deactivate"</verify>
  <acceptance_criteria>
    - `toolbox/models.py` defines a typed audit event model and query/result model
    - `toolbox/registry.py` contains persistent audit append/read helpers
    - audit persistence is bounded and does not require manual state-file editing
    - the storage contract preserves recent history even after a toolset is unregistered
  </acceptance_criteria>
  <done>Audit models and storage primitives exist and are test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Emit audit events from lifecycle and runtime-failure paths</name>
  <files>toolbox/service.py, tests/test_service.py</files>
  <read_first>toolbox/service.py, toolbox/transport_manager.py, tests/test_service.py</read_first>
  <action>Instrument `register_toolset`, `unregister_toolsets`, `activate_toolsets`, `refresh_toolsets`, `deactivate_toolsets`, and mounted-tool failure handling so each meaningful success/failure path emits one structured audit event. Use existing error codes where available instead of inventing parallel strings. Ensure timeout and transport-failure cases record enough context for recovery decisions, and make unregistration emit an event without purging the retained history.</action>
  <verify>python -m pytest -q tests/test_service.py -k "timeout or runtime_exit or register_toolset or deactivate or refresh"</verify>
  <acceptance_criteria>
    - successful registration, activation, refresh, deactivation, and unregistration each emit audit events
    - timeout and transport failure paths emit failure audit events with structured error codes
    - recreating a service from the same workspace can still read prior audit history
    - unregistration does not erase the retained audit trail for that namespace
  </acceptance_criteria>
  <done>Lifecycle emission is wired and regression-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in regression coverage for persistence and failure logging</name>
  <files>tests/test_service.py</files>
  <read_first>tests/test_service.py</read_first>
  <action>Add focused tests that prove audit history persists across service recreation, that failure paths emit events instead of only returning errors, and that event count/order are deterministic enough for host-facing querying. Prefer service-level tests for direct inspection and at least one server-level assertion for compatibility.</action>
  <verify>python -m pytest -q tests/test_service.py</verify>
  <acceptance_criteria>
    - test suite contains explicit audit persistence coverage
    - test suite contains explicit runtime failure audit coverage
    - new tests fail if event emission is removed from lifecycle methods
  </acceptance_criteria>
  <done>Audit persistence and emission are protected by regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q` passes
- [ ] audit history persists across service recreation
- [ ] audit history persists across recent unregistration
- [ ] failure events record structured error information
</verification>

<success_criteria>

- All tasks completed
- Audit persistence exists and is bounded
- Lifecycle and runtime failure paths emit structured events
- No existing control-plane flows regress

</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-observability/01-01-SUMMARY.md`
</output>
