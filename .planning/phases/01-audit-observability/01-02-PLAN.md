---
phase: 01-audit-observability
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - toolbox/service.py
  - toolbox/server.py
  - README.md
  - tests/test_service.py
autonomous: true
requirements:
  - OBS-01
  - OBS-03
  - OBS-04
user_setup: []
must_haves:
  truths:
    - "Host can query recent audit events through the Toolbox control plane with useful filters"
    - "`get_toolset_status` surfaces recent failure context without requiring raw event parsing"
    - "Audit query results stay compact and metadata-first rather than widening the always-on tool surface"
    - "Hosts can still inspect recent events for recently unregistered namespaces until retention eviction"
  artifacts:
    - path: "toolbox/service.py"
      provides: "Audit query and status-summary logic"
      contains: "audit"
    - path: "toolbox/server.py"
      provides: "MCP audit query tool"
      contains: "audit"
    - path: "README.md"
      provides: "Documented observability surface"
      contains: "audit"
  key_links:
    - from: "toolbox/server.py"
      to: "toolbox/service.py"
      via: "audit query control-plane tool"
      pattern: "audit"
---

<objective>
Expose the new audit history through a compact control-plane query surface and enrich toolset status with recent failure summaries.

Purpose: Hosts need observability they can actually consume, not just persisted events sitting on disk.
Output: Service query API, MCP audit query tool, status summary enrichment, docs, and regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit-observability/01-CONTEXT.md
@.planning/phases/01-audit-observability/01-01-PLAN.md
@toolbox/service.py
@toolbox/server.py
@README.md
@tests/test_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact audit query APIs in the service layer</name>
  <files>toolbox/service.py, tests/test_service.py</files>
  <read_first>toolbox/service.py, tests/test_service.py, spec.med</read_first>
  <action>Add a service-level audit query method that returns recent events with optional filters for namespace, operation, success/failure, and registration presence. Keep the result compact and metadata-first. Also extend `get_toolset_status` with recent-failure summary fields derived from the audit history so hosts can make quick decisions without reading the full event stream.</action>
  <verify>python -m pytest -q tests/test_service.py -k "audit and status"</verify>
  <acceptance_criteria>
    - service exposes a filtered audit query method
    - `get_toolset_status` includes recent failure summary fields
    - audit query responses remain compact and do not expose unrelated tool schemas
    - filtered queries can still return retained history for recently unregistered namespaces
  </acceptance_criteria>
  <done>Service-level observability queries exist and are covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Expose audit queries through the MCP control plane</name>
  <files>toolbox/server.py, tests/test_service.py</files>
  <read_first>toolbox/server.py, toolbox/service.py, tests/test_service.py</read_first>
  <action>Add a dedicated control-plane tool for listing audit events. Follow the existing server pattern: typed result payload, no direct file access, and tool-list stability. Include at least one full MCP integration test proving that a client can trigger lifecycle activity, query audit events, unregister a toolset, and still see retained structured history.</action>
  <verify>python -m pytest -q tests/test_service.py -k "server and audit"</verify>
  <acceptance_criteria>
    - server exposes a lifecycle audit query tool
    - MCP integration coverage proves end-to-end audit visibility
    - the new tool does not interfere with mounted-tool listing or invocation
    - the server-level audit query contract is clear for both registered and recently unregistered namespaces
  </acceptance_criteria>
  <done>MCP control-plane observability is available and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Document the observability surface and host usage</name>
  <files>README.md</files>
  <read_first>README.md, spec.med</read_first>
  <action>Update the README so the current slice explains the audit query surface, what kinds of lifecycle events are recorded, and how hosts should use recent-failure summaries versus raw audit history. Keep the explanation concise and focused on real host workflows.</action>
  <verify>Get-Content README.md</verify>
  <acceptance_criteria>
    - README mentions audit querying and recent failure summaries
    - documentation explains when to use status summary versus full audit history
  </acceptance_criteria>
  <done>Docs reflect the new observability surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest -q` passes
- [ ] MCP clients can query recent audit events
- [ ] `get_toolset_status` includes recent failure summary data
</verification>

<success_criteria>

- All tasks completed
- Hosts can query and filter audit history through Toolbox
- Status responses surface recent failure context directly
- Documentation reflects the new observability model

</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-observability/01-02-SUMMARY.md`
</output>
