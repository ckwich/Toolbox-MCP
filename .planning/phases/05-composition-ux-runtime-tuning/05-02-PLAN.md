# 05-02 Plan: Host-Facing Composition Docs, Examples, and Helper Affordances

## Goal

Finish Phase 5 by covering:

- `COMP-02`: program callers can request only the specific variables and contract
  summaries they need
- `COMP-03`: the repo includes concrete host-facing discovery and composition examples

## Planned Changes

1. Extend `run_tool_program` so callers can request selected mounted-tool contract
   summaries back alongside selected variables.
2. Add regression coverage at both service and MCP-server levels for the new return path.
3. Add a host-facing composition guide with concrete progressive-discovery, batch, and
   scripted-program examples.
4. Update README, handoff docs, roadmap, requirements, and state artifacts together.

## Verification

- Targeted pytest coverage for the new `run_tool_program` return-contract-summary path
- Full `python -m pytest -q`
- Strict warning lane: `python -m pytest -q -W error::pytest.PytestUnraisableExceptionWarning`
- `python -m compileall toolbox tests docs`
- Re-read the changed code paths after the green run
