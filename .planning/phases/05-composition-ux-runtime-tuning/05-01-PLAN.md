# 05-01 Plan: Mounted-Tool Description and Runtime-Budget Inspection

## Goal

Deliver the first Phase 5 slice by covering:

- `COMP-01`: cheap mounted-tool description before composition
- `COMP-04`: inspectable runtime budget settings

## Planned Changes

1. Add typed models for:
   - mounted-tool description payloads
   - program and transport budget inspection payloads
2. Add service helpers that:
   - describe currently mounted tools with compact per-tool summaries
   - expose current program/runtime budget settings
3. Expose those helpers through the Toolbox MCP control plane.
4. Add regression coverage at both service and MCP-server levels.
5. Update README and phase/state artifacts together so the repo stays deliberate.

## Verification

- Targeted pytest coverage for the new description and budget surfaces
- Full `python -m pytest -q`
- `python -m compileall toolbox tests`
- Re-read the changed code paths after the green run to catch logic drift
