# 06-01 Plan: Release, CI, and Documentation Polish

## Goal

Finish the post-v1 polish slice by covering:

- `OPS-01`: cross-platform CI keeps the verified runtime surface honest
- `OPS-02`: top-level docs show the shortest successful first-run path
- `OPS-03`: state compatibility and protected transport persistence are documented
- `OPS-04`: handoff docs reflect the current repo, not the original bootstrap intent

## Planned Changes

1. Add a GitHub Actions workflow that runs the main verification lane on Windows and
   Linux, plus the strict Windows unraisable-warning lane.
2. Add a focused quickstart guide for the seeded fake toolset flow and link it from the
   top-level README.
3. Extend the README with a concise first-run path plus explicit notes about state
   versioning, migration, and transport-secret persistence.
4. Replace the stale handoff doc with a current operational/developer handoff.
5. Update roadmap, state, requirements, and project artifacts together so the polish
   pass is tracked deliberately and closed out cleanly.

## Verification

- Re-read the changed planning and top-level docs for consistency and drift
- `python -m pytest -q`
- `python -m pytest -q -W error::pytest.PytestUnraisableExceptionWarning`
- `python -m compileall toolbox tests`
- Re-read the workflow file and top-level docs after the green run
